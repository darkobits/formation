/**
 * Endpoint used for "API" requests.
 *
 * @type {string}
 */
export const API_ENDPOINT = '/api';


/**
 * Emulated API delay. Used to show form(s) disabling during pending HTTP
 * requests.
 *
 * @type {number}
 */
export const API_DELAY = 1000;
